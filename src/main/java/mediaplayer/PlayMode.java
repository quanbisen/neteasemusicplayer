package mediaplayer;

public enum PlayMode {
    /**SEQUENCE         顺序播放
     * SEQUENCE_LOOP    顺序循环
     * SINGLE_LOOP      单曲循环
     * SHUFFLE          随机播放
     * */
    SEQUENCE,
    SEQUENCE_LOOP,
    SINGLE_LOOP,
    SHUFFLE
}
